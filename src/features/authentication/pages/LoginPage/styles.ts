import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  formCard: {
    padding: 30,
    minWidth: 400,
  },
  cardSection: {
    padding: 20,
  },
  fullSize: {
    height: "100%",
    width: "100%",
  },
}));
